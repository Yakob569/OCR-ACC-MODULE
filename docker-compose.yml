services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: receipt-ocr-backend:latest
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./tests:/app/tests
    env_file:
      - .env.example
